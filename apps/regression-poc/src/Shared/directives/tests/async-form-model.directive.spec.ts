import { AsyncFormModelDirective } from '../async-form-model.directive';

describe('AsyncFormModelDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncFormModelDirective();
    expect(directive).toBeTruthy();
  });
});
